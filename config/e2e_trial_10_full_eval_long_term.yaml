base_config: "training_config.yaml"

run_mode:
  mode: "production"

data:
  time_range:
    start_date: "2024-01-01"
    end_date: "2024-01-08"
  asset_pairs:
    correlated_assets: []
  validation:
    fail_on_invalid: false
    max_gap_seconds: 600

model:
  long_term:
    enabled: true
    windows_days: [1, 3, 7]

training:
  epochs: 1
  batch_size: 8
  debug_max_samples: 100000

hyperparameter_optimization:
  enabled: false

snapshot:
  root_name: "e2e_trials_v2"
  name: "trial_10_full_eval_long_term_v2"

evaluation:
  calibration_analysis:
    enabled: true
  post_hoc_calibration:
    enabled: true
  temporal_degradation:
    enabled: true
  backtesting:
    enabled: true

mlflow:
  artifact_logging:
    trained_model: false
  model_registry:
    register_model: false

diagnostics:
  enabled: false
