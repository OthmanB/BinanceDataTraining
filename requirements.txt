# Core dependencies for Binance ML Training Platform
# Versions chosen for compatibility with the current environment (TensorFlow 2.16.x + bundled Keras)

# YAML and utilities
pyyaml==6.0.2
termcolor==2.4.0
requests==2.32.3

# MLFlow for experiment tracking
mlflow==2.22.4

# Optuna for hyperparameter optimization
optuna==3.6.1

# Calibration utilities
scipy==1.11.4

# Property-based testing (reduced examples in CI via CI=true env var)
hypothesis>=6.100.0

# TensorFlow + Keras (GPU/CPU variant can be adjusted if needed)
# TensorFlow 2.16.x supports Python 3.9–3.12 and uses tf.keras bundled with TensorFlow.
# The standalone keras package is not installed separately.
tensorflow==2.15.0; sys_platform == "darwin" and python_version >= "3.9" and python_version < "3.12"
tensorflow==2.15.0; sys_platform == "linux" and python_version >= "3.9" and python_version < "3.12"
tensorflow==2.16.2; sys_platform == "darwin" and python_version >= "3.12" and python_version < "3.13"
tensorflow==2.16.2; sys_platform == "linux" and python_version >= "3.12" and python_version < "3.13"

# Optional: if you later use numpy/pandas explicitly, they will be pulled in by TF/MLflow,
# but you can pin them here if you want deterministic versions.
# numpy==1.26.4
# pandas==2.2.2

# tests
pytest>=9.0.2
